machine:
  environment:
    RAKEOPT: -t
general:
  build_dir: dashboard
test:
  override:
    - bundle exec rake test:
        parallel: true
        files:
          - test/**/*_test.rb
          - ../pegasus/test/test_*.rb
          - ../shared/test/test_*.rb
