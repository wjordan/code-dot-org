general:
  build_dir: dashboard
dependencies:
  bundler:
    without: [development]
database:
  post:
    - RAILS_ENV=test bundle exec rake seed:all -t
test:
  minitest_globs:
    - test/**/*_test.rb
    - ../pegasus/test/test_*.rb
    - ../shared/test/test_*.rb
