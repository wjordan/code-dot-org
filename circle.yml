general:
  build_dir: dashboard
dependencies:
  bundler:
    without: [development]
test:
  pre:
    - bundle exec rake seed:all
  minitest_globs:
    - test/**/*_test.rb
    - ../pegasus/test/test_*.rb
    - ../shared/test/test_*.rb
